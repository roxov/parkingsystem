package com.parkit.parkingsystem.util;

public class DateTimeFormatter {

	/*
	 * public Timestamp convertLDTToSQLTimestamp (LocalDateTime ldt) { //ldt = new
	 * Timestamp(localDateTime.toDateTime().getMillis());
	 * 
	 * return ts ; }
	 * 
	 * // Timestamp ts = Timestamp.valueOf(dateTime);
	 * 
	 * public LocalDateTime convertSQLTimestampToLDT (Timestamp ts) { // ldt = new
	 * LocalDateTime(); //ldt = Timestamp.toLocalDateTime(); //LocalDateTime ldt =
	 * LocalDateTime. //DateTimeFormatter dtf =
	 * DateTimeFormatter.forPattern("yyyy-MM-dd HH:mm:ss"); //Timestamp ts =
	 * Timestamp.valueOf(ldt.toString(dtf)); return ldt ; }
	 */
}
